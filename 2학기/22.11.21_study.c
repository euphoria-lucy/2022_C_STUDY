/* STUDY_01 
#include <stdio.h>

void iterate(int* arr, int arr_size) {
        for (int i = 0; i < arr_size; i++) {
            printf("%d\n", *(arr + i));
        }
}

int main() {
    int arr[] = { 1, 2, 3, 4, 5 };
    iterate(arr, 5);
} */